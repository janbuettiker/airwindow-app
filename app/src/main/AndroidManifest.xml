<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.airwindow_app">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Airwindowapp"
        tools:targetApi="31">
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false"
            android:label="Home Overview"
            android:parentActivityName=".activities.HomeActivity" />
        <activity
            android:name=".activities.RoomActivity"
            android:exported="false"
            android:label="Room Overview"
            android:parentActivityName=".activities.RoomActivity" />
        <activity
            android:name=".activities.WindowActivity"
            android:exported="false"
            android:label="Window Settings"
            android:parentActivityName=".activities.WindowOverviewActivity" />
        <activity
            android:name=".activities.WindowOverviewActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>